<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
            https://maven.apache.org/xsd/assembly-2.2.0.xsd">

  <id>bundle</id>

  <formats>
    <format>zip</format>
  </formats>

  <!-- Do not wrap contents in a top-level directory -->
  <includeBaseDirectory>false</includeBaseDirectory>

  <!-- Include the fat JAR and pyproject.toml at the root of the zip -->
  <files>
    <file>
      <source>${project.build.directory}/${project.artifactId}-${project.version}.jar</source>
      <outputDirectory>/</outputDirectory>
    </file>
    <file>
      <source>pyproject.toml</source>
      <outputDirectory>/</outputDirectory>
    </file>
  </files>

  <!-- Include all Python source files under a python/ directory -->
  <fileSets>
    <fileSet>
      <directory>src/main/python</directory>
      <outputDirectory>python</outputDirectory>
      <includes>
        <include>*.py</include>
      </includes>
    </fileSet>
  </fileSets>

</assembly>
